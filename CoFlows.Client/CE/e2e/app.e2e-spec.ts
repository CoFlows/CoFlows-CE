import { DecimaPage } from './app.po';

describe('CoFlows', () => {
  let page: DecimaPage;

  beforeEach(() => {
    page = new DecimaPage();
  });

  it('should expect true to be true', () => {
    expect(true).toBe(true);
  });
});
